<div class="page-header">
  <%= link_to backstage_pages_path, class: 'btn btn-default' do %>
    All Backstage_pages
  <% end %>
  <%= link_to edit_backstage_page_path(@backstage_page), class: 'btn btn-primary' do %>
    Edit
  <% end %>
  <h1>Show backstage_page</h1>
</div>

<dl class="dl-horizontal">
  <dt>Title:</dt>
  <dd><%= @backstage_page.title %></dd>

  <dt>Body:</dt>
  <dd><%= @backstage_page.body %></dd>

</dl>
